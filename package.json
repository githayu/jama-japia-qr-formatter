{
  "name": "jama-japia-qr-formatter",
  "description": "Format the QR code of the standard ledger sheet guideline.",
  "version": "0.0.0-development",
  "scripts": {
    "test": "jest --coverage",
    "test:ci": "jest",
    "build": "rm -rf ./dist && yarn build:common && yarn build:esm",
    "build:common": "tsc -p tsconfig.common.json",
    "build:esm": "tsc -p tsconfig.esm.json",
    "semantic-release": "semantic-release"
  },
  "license": "MIT",
  "module": "./dist/esm/index.js",
  "types": "./dist/esm/index.d.ts",
  "main": "./dist/index.js",
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "semantic-release": "^17.4.1",
    "ts-jest": "^26.5.3",
    "typescript": "^4.2.3"
  },
  "author": {
    "name": "Hayu",
    "email": "admin@hayu.io"
  },
  "repository": "github:githayu/jama-japia-qr-formatter",
  "files": [
    "dist"
  ]
}
